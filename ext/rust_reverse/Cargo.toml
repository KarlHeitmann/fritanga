[package]
name = "rust_reverse"
version = "0.9.75"
autotests = true # set true if you want to use "cargo test"
edition = "2018"

[dependencies]
magnus = "0.5"

[lib]
crate-type = ["cdylib"]

[features]
test-feature = []
